# FinderPathBar

面向习惯 Windows「资源管理器地址栏」的 macOS 用户：在 Finder 窗口上方贴一条轻量地址栏，显示当前路径，并提供新建、重命名、剪切移动、收藏夹等快捷操作。

**当前版本：1.0.105**

## 下载

请到本仓库 **[Releases](https://github.com/yikeshu0611/FinderPathBar/releases)** 页面，在 **Assets** 中下载最新 `.dmg`（例如 `FinderPathBar-1.0.105.dmg`）。

## 安装与权限

1. 打开 DMG，将 `FinderPathBar.app` 拖到「应用程序」
2. 若系统拦截：系统设置 → 隐私与安全性 → **仍要打开**
3. 授予 **辅助功能** 权限（按提示也可开自动化），然后打开 Finder，地址栏会附着在窗口上方

## 主要功能

- **地址栏附着**：跟随当前 Finder 窗口，也支持其它 App 的打开/上传/保存/新建项目对话框；`⌘L` 编辑路径，支持补全与回车跳转；第一行 **Xo** 关闭其他窗口、**Xa** 关闭全部窗口
- **路径导航**：上级目录、前进/后退、剪贴板路径跳转（goPth）、复制路径（CPth）
- **新建文件**：第 2 行 `dir / txt / …` 单击新建、双击新建并打开；可在设置中自定义 6 个后缀（`dir` = 文件夹）
- **文件操作**：F2 重命名、打开、复制 / 剪切 / 粘贴移动、废纸篓与撤销、Ext（扩展名注释）
- **当前文件夹搜索**：`⌘F` 按名称过滤
- **收藏夹**：固定在第 2 行，点 ★ 添加，支持分组菜单
- **设置**：图标/文字尺寸、背景色、开机启动、语言、新建类型、检查更新等
- **赞赏提示**：扫码支持；关闭后约 10 分钟再提示，「1天后出现」可暂缓一天；赞赏后点击「永久关闭」仅本地不再弹出

## 1.0.105 更新摘要

- 支持「新建项目」类对话框（如 RStudio 新建 R 项目）：识别「创建」按钮与「存储为」面板，并正确显示文稿等常用目录路径

## 1.0.104 更新摘要

- 单击新建文件/文件夹后自动进入重命名状态（双击新建并打开不受影响）

## 1.0.103 更新摘要

- 修复打开对话框中点 × 误触「显示选项」：改为只按 Cancel（取消），不再误按第一个按钮

## 1.0.102 更新摘要

- 修复打开/保存对话框中点左上角 × 无法关闭：改为关闭当前对话框（标题栏关闭 / 取消 / Esc）

## 1.0.101 更新摘要

- 打开对话框中收藏夹 / 历史等跳转改为与「上级」同类的相对导航（先 ⌘↑，再双击进入子文件夹）；仅在失败时才兜底 ⌘⇧G

## 1.0.100 更新摘要

- 修复打开/保存对话框中点「返回 / 前进」会跳到 Finder：改为在对话框内跳转历史路径

## 1.0.99 更新摘要

- 修复打开对话框从收藏夹跳转时连点 Open、蜂鸣甚至关掉对话框：取消逐级 ⌘↓，改为一次跳转；确认用「前往」按钮而非回车误触 Open

## 1.0.98 更新摘要

- 打开/保存对话框中，历史记录、收藏夹、地址栏回车、路径段等跳转改为相对导航（⌘↑ + 进入子文件夹），不再先弹出「前往文件夹」

## 1.0.97 更新摘要

- 打开/保存对话框中点「上级」改为 ⌘↑ 直接进入上一级，不再弹出「前往文件夹」

## 1.0.96 更新摘要

- Finder 回到前台时，地址栏会立刻从打开/保存对话框切回 Finder，不再粘在后面的上传窗口上

## 1.0.95 更新摘要

- 修复「检查更新」因 GitHub API 返回 403 而失败：改用临时网络会话，并增加网页跳转 / Atom 备用检查

## 1.0.94 更新摘要

- 新建 `xlsx` / `docx` / `pptx` 改为真正的空白 Office 文档（不再生成空文件）
- 补全 xlsx 的主题、样式与文档属性，方便 Excel / WPS 正常打开

## 1.0.93 更新摘要

- 出现「替换 / 保留两者 / 停止」或简介等确认框时，暂时隐藏地址栏；关闭后自动恢复

## 1.0.92 更新摘要

- 打开/保存对话框上的地址栏改到窗口上方，不再遮挡原生标题栏

## 1.0.91 更新摘要

- 支持在其它 App 的「打开 / 上传 / 保存」文件对话框上显示地址栏：可编辑路径、上级、收藏夹跳转（通过 ⌘⇧G 前往文件夹）

## 1.0.90 更新摘要

- 点击上级（^）返回后，Finder 会选中刚离开的那个文件夹

## 1.0.89 更新摘要

- 设置默认值：图标大小 20，文字大小 15

## 1.0.88 更新摘要

- 搜索结果不再默认选中第一项；用方向键或鼠标再选择

## 1.0.87 更新摘要

- 单击新建文件/文件夹后，Finder 会选中刚创建的项目

## 1.0.86 更新摘要

- 第一行三个 x（关闭、Xo、Xa）使用同一套绘制，大小一致且下边缘对齐
- 点「更新」后显示下载进度条（百分比与已下/总量）

## 1.0.85 更新摘要

- Xo / Xa 的 o、a 与 x 下边缘对齐，并略向右移

## 1.0.84 更新摘要

- Xo / Xa 的 x 与第一个关闭按钮同样大小；o / a 略加大

## 1.0.83 更新摘要

- Xo / Xa 改为大写 X 加右下角下标 o / a

## 1.0.82 更新摘要

- Xo / Xa 的 o、a 放到 x 右下角，并略加大

## 1.0.81 更新摘要

- Xo / Xa 的 o、a 缩小为约 x 的 1/3

## 1.0.80 更新摘要

- Xo / Xa 的 x 与第一个关闭按钮同样大小
- 修复点 Xo 无法关闭其他 Finder 窗口的问题

## 1.0.79 更新摘要

- 第一行增加 **Xo**（关闭其他 Finder 窗口）和 **Xa**（关闭所有 Finder 窗口）

## 1.0.78 更新摘要

- 修复从 Finder 复制文件后，在其他软件里 ⌘V 要按两次才能粘贴（文字粘贴不受影响）

## 1.0.77 更新摘要

- 不再把 ⌘C / ⌘X / ⌘V 注册成全局热键

## 1.0.76 更新摘要

- 修复点击地址栏 / 收藏夹后地址栏经常闪一下消失再出现的问题

## 1.0.75 更新摘要

- 修复点「更新」后应用退出、安装脚本被一起杀掉、无法真正换上新版本的问题

## 1.0.74 更新摘要

- Ext 右侧恢复灰色竖线，与收藏夹分开

## 1.0.73 更新摘要

- 自动更新改为**每月检查一次**；发现新版本时弹窗询问是否更新（不再静默安装）
- 设置里「检查更新」仍可随时手动检查，有新版本同样会询问

## 1.0.72 更新摘要

- 设置中新增「检查更新」：从 GitHub Releases 检测新版本并自动下载 DMG 替换安装、重启
- 启动约 8 秒后也会静默检查；有新版本时自动更新

## 1.0.71 更新摘要

- 移除赞赏相关的 `example.png` 示例截图资源与打包引用

## 1.0.70 更新摘要

- 去掉 Ext 右侧多余空隙，收藏夹更紧凑贴在操作栏旁
- 赞赏弹窗：点「关闭」约 **10 分钟**后再出现；原「3天不显示」改为 **「1天后出现」**
- 设置中可自定义第 2 行 6 个新建类型后缀
- 设置中移除收藏夹行位置 / 导入 / 导出；收藏夹固定第 2 行
- 赞赏「永久关闭」改为本地提示按钮（无邮件流程）
- 回车键在 Finder 前台时正确绑定打开 / 重命名确认

## 开发与打包

源码在 `FinderPathBar/`：

```bash
cd FinderPathBar && ./build.sh
```

产物：`dist/FinderPathBar-<version>.dmg`

## 反馈

使用问题或建议，请提交 [Issues](https://github.com/yikeshu0611/FinderPathBar/issues)，或将日志目录  
`~/Documents/FinderPathBar/` 中的当日日志发给作者邮箱 `zj391120@163.com`。

## License

个人工具开源分享；使用前请自行评估权限与安全性。未公证安装包在部分系统上需手动允许运行。
